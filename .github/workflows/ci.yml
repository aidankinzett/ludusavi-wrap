name: CI

on:
  # Push only on master so Swatinem/rust-cache saves a cache in the
  # default-branch scope — tag-triggered release builds can only restore
  # caches from the default branch. Feature-branch validation goes
  # through pull_request to avoid the push+PR double-run.
  push:
    branches:
      - master
    paths:
      - 'tauri/**'
      - 'decky/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - master
    paths:
      - 'tauri/**'
      - 'decky/**'
      - '.github/workflows/ci.yml'

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      # Mirror release.yml so this workflow populates the same sccache
      # entries that release.yml will restore on tag pushes.
      SCCACHE_DIR: "${{ github.workspace }}/.sccache"
      RUSTC_WRAPPER: "sccache"
      CARGO_INCREMENTAL: "0"
      # Workspace-local bun cache dir so actions/cache can persist it with
      # one OS-agnostic path.
      BUN_INSTALL_CACHE_DIR: "${{ github.workspace }}/.bun-cache"
    defaults:
      run:
        shell: pwsh

    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.x"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Enable LLD linker
        # link.exe is the slow, uncached part of the Windows build (sccache
        # only caches rustc codegen, never linking). Point rustc at LLVM's
        # lld-link (the MSVC-flavour LLD, preinstalled on the runner) to cut
        # link time across the test binaries and the release build. Probe
        # first so a runner-image change that drops LLVM falls back to
        # link.exe instead of breaking the build. Keep this identical to
        # release.yml so both share the same sccache keys (RUSTFLAGS is part
        # of the compile-cache hash).
        run: |
          $llvm = "C:\Program Files\LLVM\bin"
          if (Test-Path "$llvm\lld-link.exe") {
            "$llvm" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
            "RUSTFLAGS=-C linker=lld-link" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
            Write-Host "Using lld-link from $llvm"
          } else {
            Write-Warning "lld-link not found; falling back to link.exe"
          }

      - name: Bun cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.bun-cache
          key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ runner.arch }}-

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Sccache cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tauri/src-tauri -> target
          # sccache owns object caching; rust-cache only does registry.
          cache-targets: "false"
          # Fixed key (not the job name) so the tag-triggered release
          # build restores the same registry cache this populates.
          shared-key: spool-windows

      - name: Install frontend dependencies
        working-directory: tauri
        run: bun install --frozen-lockfile

      - name: Cache sidecars
        id: cache-sidecars
        uses: actions/cache@v6
        with:
          path: tauri/src-tauri/binaries
          key: sidecars-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/scripts/download-sidecars.js') }}

      - name: Download sidecars
        if: steps.cache-sidecars.outputs.cache-hit != 'true'
        working-directory: tauri
        run: bun run download-sidecars

      # `cargo clippy --all-targets` compiles the same crates as
      # `cargo check` and emits the same errors plus lints, so a separate
      # check step is pure redundant compilation — dropped. Frontend
      # checks (svelte-check / eslint / vitest) moved to the `frontend`
      # job on Ubuntu, off the slow Windows critical path.
      #
      # This job stays the authoritative lint for the cfg(windows) code
      # (registry.rs, launcher.rs, the Windows arms of process.rs/ludusavi.rs)
      # — clippy only ever sees the code compiled for its own target, so the
      # fast clippy-linux job can't cover it. --all-targets still compiles the
      # cfg(windows) test targets here too, so test *execution* moving to
      # clippy-linux loses no compile coverage (the suite has no cfg(windows)
      # tests).
      - name: Cargo clippy
        working-directory: tauri/src-tauri
        run: cargo clippy --all-targets -- -D warnings

      - name: Tauri build (no bundle)
        # master pushes only. This is the slow release-profile compile —
        # PRs get fast feedback from check/clippy/test above, while master
        # smoke-tests the full app build and warms the release-profile
        # sccache cache that release.yml restores on tag pushes. Skip
        # bundle generation; release.yml produces the artifacts.
        if: github.event_name == 'push'
        working-directory: tauri
        run: bun run tauri build --no-bundle

  # Frontend checks (svelte-check / eslint / vitest). Platform-independent,
  # so they run on a fast Ubuntu runner in parallel instead of sitting in
  # the critical path of the slow Windows build.
  frontend:
    runs-on: ubuntu-latest
    env:
      BUN_INSTALL_CACHE_DIR: "${{ github.workspace }}/.bun-cache"
    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.x"

      - name: Bun cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.bun-cache
          key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ runner.arch }}-

      - name: Install frontend dependencies
        working-directory: tauri
        run: bun install --frozen-lockfile

      - name: Svelte check
        working-directory: tauri
        run: bun run check

      - name: ESLint
        working-directory: tauri
        run: bun run lint

      - name: Frontend tests
        working-directory: tauri
        run: bun run test

      # Storybook story tests run in a real browser (Vitest browser mode via
      # Playwright), so they're a separate step that provisions Chromium first
      # instead of riding on the browser-free `bun run test` above.
      - name: Install Playwright Chromium
        working-directory: tauri
        run: bunx playwright install --with-deps chromium

      - name: Storybook story tests
        working-directory: tauri
        run: bun run test:storybook

  # Backend lint + unit tests on a fast Ubuntu runner. Dev-profile clippy
  # compiles far quicker than the release-profile Windows job, so PRs get
  # their first green backend signal here while build-windows runs in
  # parallel as the cfg(windows) backstop. This is also the only job that
  # lints the cfg(target_os = "linux") code (proton.rs, gamemode.rs,
  # session.rs, decky_install.rs) — the Windows clippy never compiles it.
  # The platform-independent test suite runs here too (it has no
  # cfg(windows) tests), off the slow Windows path. Runs on PRs and master.
  clippy-linux:
    runs-on: ubuntu-22.04
    env:
      SCCACHE_DIR: "${{ github.workspace }}/.sccache"
      RUSTC_WRAPPER: "sccache"
      CARGO_INCREMENTAL: "0"
      BUN_INSTALL_CACHE_DIR: "${{ github.workspace }}/.bun-cache"

    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.x"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Sccache cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-${{ runner.os }}-${{ runner.arch }}-clippy-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            sccache-${{ runner.os }}-${{ runner.arch }}-clippy-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tauri/src-tauri -> target
          cache-targets: "false"
          shared-key: spool-linux-clippy

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev libudev-dev

      - name: Bun cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.bun-cache
          key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ runner.arch }}-

      - name: Install frontend dependencies
        working-directory: tauri
        run: bun install --frozen-lockfile

      - name: Cache sidecars
        id: cache-sidecars
        uses: actions/cache@v6
        with:
          path: tauri/src-tauri/binaries
          key: sidecars-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/scripts/download-sidecars.js') }}

      - name: Download sidecars
        if: steps.cache-sidecars.outputs.cache-hit != 'true'
        working-directory: tauri
        run: bun run download-sidecars

      - name: Cargo clippy
        working-directory: tauri/src-tauri
        run: cargo clippy --all-targets -- -D warnings

      - name: Cargo test
        working-directory: tauri/src-tauri
        run: cargo test --all

  # Mirrors the `build-linux` job in release.yml so the rust-cache key
  # matches. master pushes only — this job is a pure release-profile
  # build (no separate lint/test steps), so it both smoke-tests the
  # Linux build and populates the Linux cache that the release job
  # restores on tag pushes. PRs skip it for fast feedback.
  build-linux:
    if: github.event_name == 'push'
    # Mirrors release.yml's arch matrix so ARM breakage surfaces on master
    # rather than at tag time. ARM AppImages cannot be cross-compiled, so
    # aarch64 needs its own native runner here too.
    name: build-linux (${{ matrix.appimage_arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            appimage_arch: x86_64
          - runner: ubuntu-22.04-arm
            appimage_arch: aarch64
    env:
      SCCACHE_DIR: "${{ github.workspace }}/.sccache"
      RUSTC_WRAPPER: "sccache"
      CARGO_INCREMENTAL: "0"
      BUN_INSTALL_CACHE_DIR: "${{ github.workspace }}/.bun-cache"

    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.x"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Sccache cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tauri/src-tauri -> target
          cache-targets: "false"
          shared-key: spool-linux

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libudev-dev

      - name: Bun cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.bun-cache
          key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ runner.arch }}-

      - name: Install frontend dependencies
        working-directory: tauri
        run: bun install --frozen-lockfile

      # The Spool binary embeds the Decky plugin bundle (Linux only) via
      # include_str!; build it so the genuine bundle is what gets compiled in.
      # bun is the same toolchain the Tauri frontend already uses above.
      - name: Build Decky plugin
        working-directory: decky
        run: |
          bun install --frozen-lockfile
          bun run build

      - name: Cache sidecars
        id: cache-sidecars
        uses: actions/cache@v6
        with:
          path: tauri/src-tauri/binaries
          key: sidecars-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/scripts/download-sidecars.js', 'tauri/scripts/build-ludusavi.sh') }}

      - name: Download sidecars
        if: steps.cache-sidecars.outputs.cache-hit != 'true'
        working-directory: tauri
        run: bun run download-sidecars

      # ludusavi publishes no aarch64 Linux binary; compile it at the pin.
      - name: Build ludusavi sidecar (aarch64)
        if: matrix.appimage_arch == 'aarch64' && steps.cache-sidecars.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install -y gcc libxcb-composite0-dev libgtk-3-dev
          bash tauri/scripts/build-ludusavi.sh

      - name: Tauri build (no bundle)
        working-directory: tauri
        run: bun run tauri build --no-bundle

  # WebDriver end-to-end smoke tests against the real Tauri window via
  # tauri-driver (WebKitWebDriver under Xvfb). Runs on PRs and master so UI
  # regressions are gated before merge. Uses the same SCCACHE_DIR filesystem
  # backend as build-linux (not the GHA backend) and restores its caches via
  # the shared `sccache-Linux-X64-` restore-keys, so the release build it needs
  # actually reuses build-linux's compiled objects instead of cold-compiling
  # a parallel GHA cache. Saves under its own `-e2e-` key to avoid colliding
  # with build-linux's save, and keeps its own rust-cache registry key for
  # the same reason.
  e2e-linux:
    runs-on: ubuntu-22.04
    env:
      SCCACHE_DIR: "${{ github.workspace }}/.sccache"
      RUSTC_WRAPPER: "sccache"
      # Fall back to the real compiler when sccache can't reach its
      # backend. The GitHub Actions cache service has outages; without
      # this, a failed cache read at server startup aborts every rustc
      # call and breaks the build. With it, the run is just slower.
      SCCACHE_IGNORE_SERVER_IO_ERROR: "1"
      CARGO_INCREMENTAL: "0"
      BUN_INSTALL_CACHE_DIR: "${{ github.workspace }}/.bun-cache"

    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.x"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Sccache cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-${{ runner.os }}-${{ runner.arch }}-e2e-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            sccache-${{ runner.os }}-${{ runner.arch }}-e2e-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-
            sccache-${{ runner.os }}-${{ runner.arch }}-

      - name: Probe sccache backend (fall back to direct compile if down)
        # SCCACHE_IGNORE_SERVER_IO_ERROR only covers failures talking to an
        # already-running server — NOT the backend self-check sccache runs
        # when the server first starts. If the start-up self-check fails,
        # every wrapped rustc call exits non-zero and breaks the build. Start
        # the server up front; if it can't initialise its backend, drop the
        # wrapper for the rest of the job (GITHUB_ENV overrides the job-level
        # env for later steps) so cargo compiles directly — uncached but
        # green.
        shell: bash
        run: |
          if sccache --start-server; then
            echo "sccache backend reachable."
          else
            echo "::warning::sccache backend unreachable; building without the compiler cache."
            echo "RUSTC_WRAPPER=" >> "$GITHUB_ENV"
          fi

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tauri/src-tauri -> target
          cache-targets: "false"
          shared-key: spool-linux-e2e

      - name: Install Linux system dependencies
        # webkit2gtk-driver provides WebKitWebDriver, which tauri-driver drives.
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libudev-dev \
            webkit2gtk-driver \
            xvfb

      # tauri-driver was compiled from source on every run (~minutes, fully
      # uncached). Pin the version and cache the built binary so it installs
      # once and restores instantly thereafter.
      - name: Cache tauri-driver
        id: cache-tauri-driver
        uses: actions/cache@v6
        with:
          path: ~/.cargo/bin/tauri-driver
          key: tauri-driver-${{ runner.os }}-${{ runner.arch }}-2.0.5
      - name: Install tauri-driver
        if: steps.cache-tauri-driver.outputs.cache-hit != 'true'
        run: cargo install tauri-driver --version 2.0.5 --locked

      - name: Bun cache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.bun-cache
          key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-${{ runner.arch }}-

      - name: Install frontend dependencies
        working-directory: tauri
        run: bun install --frozen-lockfile

      - name: Cache sidecars
        id: cache-sidecars
        uses: actions/cache@v6
        with:
          path: tauri/src-tauri/binaries
          key: sidecars-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('tauri/scripts/download-sidecars.js') }}

      - name: Download sidecars
        if: steps.cache-sidecars.outputs.cache-hit != 'true'
        working-directory: tauri
        run: bun run download-sidecars

      - name: Build app (no bundle)
        working-directory: tauri
        run: bun run e2e:build

      - name: Run E2E tests
        working-directory: tauri
        # Xvfb gives the webview a display in the headless runner.
        run: xvfb-run -a bun run e2e
